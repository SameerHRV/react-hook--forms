import USerProvider from "./users/components/USerProvider";

interface AppProps {}

const App = ({}: AppProps) => {
  return (
    <div>
      <USerProvider />
    </div>
  );
};

export default App;
